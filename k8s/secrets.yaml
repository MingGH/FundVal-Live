apiVersion: v1
kind: Secret
metadata:
  name: fundval-secrets
  namespace: fundval
type: Opaque
stringData:
  DB_PASSWORD: changeme          # 请修改
  JWT_SECRET: changeme           # 请修改为随机字符串
  ADMIN_PASSWORD: admin123       # 请修改
  OPENAI_API_KEY: ""             # 可选
  SMTP_USER: ""                  # 可选
  SMTP_PASSWORD: ""              # 可选
