name: Build and Release

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            platform: macOS
          - os: windows-latest
            platform: Windows
          - os: ubuntu-latest
            platform: Linux

    runs-on: ${{ matrix.os }}
    timeout-minutes: 60

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install uv (Unix)
        if: runner.os != 'Windows'
        run: curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Install uv (Windows)
        if: runner.os == 'Windows'
        run: irm https://astral.sh/uv/install.ps1 | iex
        shell: powershell

      - name: Install Python dependencies
        run: |
          uv pip install --system -r backend/requirements.txt
          uv pip install --system pyinstaller

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Build backend with PyInstaller
        run: uv run pyinstaller backend.spec --clean

      - name: Install Electron dependencies
        run: npm ci

      - name: Build Electron app (macOS)
        if: runner.os == 'macOS'
        run: npm run dist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Electron app (Windows)
        if: runner.os == 'Windows'
        run: npm run dist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Electron app (Linux)
        if: runner.os == 'Linux'
        run: npm run dist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts (macOS)
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: macos-build
          path: release/*.dmg

      - name: Upload artifacts (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: release/*.exe

      - name: Upload artifacts (Linux)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: |
            release/*.AppImage
            release/*.deb

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          body: |
            ## 安装包下载

            ### macOS
            - **Apple Silicon (M1/M2/M3)**: `FundVal-Live-*-arm64.dmg`
            - **Intel**: `FundVal-Live-*-x64.dmg`

            ### Windows
            - **64位**: `FundVal-Live-Setup-*.exe`

            ### Linux
            - **AppImage**: `FundVal-Live-*.AppImage`
            - **Debian/Ubuntu**: `fundval-live_*_amd64.deb`

            ## 本次更新 (v1.0.15)

            ### ✨ 新功能
            - **自动更新净值**
              - 16:00-24:00 每小时自动拉取持仓基金净值（仅交易日）
              - 只有当日净值已公布才算更新成功
              - 自动处理待确认的加仓/减仓交易

            - **手动更新净值按钮**
              - Account 页面新增「更新净值」按钮
              - 可随时手动触发净值更新
              - 显示详细更新结果（已更新/未公布/失败）

            - **净值更新状态标记**
              - 持仓明细表格显示当日净值是否已更新
              - ✓ 绿色勾：当日净值已更新
              - ⏰ 灰色钟：当日净值未公布

            - **动态分类筛选**
              - 关注列表分类按钮从 API 动态加载
              - 自动适配数据库分类，无需手动维护

            - **大类归并**
              - 32 种小分类归并为 9 种大类
              - 股票型/混合型/债券型/FOF/货币型/指数型/REITs/QDII/其他
              - 饼图自动归类，更清晰直观

            ### 🔧 优化改进
            - **分类逻辑重构**
              - 删除硬编码的 430+ 个关键词
              - 改用数据库 official_type 字段
              - 统一前后端分类逻辑

            - **性能优化**
              - 避免重复数据库查询（name 和 type 一次查询）
              - 修复数据库连接泄漏问题

            - **筛选改进**
              - 使用模糊匹配，"股票型"可匹配"股票型"、"混合型-偏股"等

            ### 🐛 Bug 修复
            - 修复 get_fund_type() 异常时连接未关闭的问题
            - 修复分类筛选精确匹配导致的误过滤

            ## 使用说明

            1. 下载对应平台的安装包
            2. 双击安装
            3. 启动应用即可使用

            数据存储位置：
            - 数据库：`~/.fundval-live/data/fund.db`
            - 日志：`~/.fundval-live/logs/`

            ## 注意事项

            - **macOS**：
              - 首次打开可能提示"无法验证开发者"，请在系统偏好设置 → 安全性与隐私中允许
              - 如提示"应用已损坏"，在终端执行：
                ```bash
                sudo xattr -dr com.apple.quarantine "/Applications/FundVal Live.app"
                ```
            - **Windows**：可能提示 SmartScreen，点击"更多信息"→"仍要运行"
            - **Linux**：AppImage 需要添加执行权限：`chmod +x FundVal-Live-*.AppImage`

            ## 开源协议

            本项目采用 AGPL-3.0 协议开源。如果你使用本项目代码提供网络服务，必须开源你的修改。
            详见：https://github.com/Ye-Yu-Mo/FundVal-Live/blob/main/LICENSE
          files: |
            macos-build/*
            windows-build/*
            linux-build/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
